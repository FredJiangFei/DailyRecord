import colors from '../config/colors';

const components = {
  Heading: {
    baseStyle: {
      textAlign: 'center',
      my: 4,
    },
  },
  Divider: {
    baseStyle: {
      bg: colors.light,
    },
  },
};

export default components;
